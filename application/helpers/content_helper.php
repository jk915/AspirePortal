<?php
	/***
	* @helper content_helper 
	* @author Andrew Chapman
	* @abstract The content helper is used to load dynamic content within a page.
	* For example, with the html body of a page, a php function in here can be invoked 
	* using the syntax [[PHP_myMethodName(params)]]
	* If a method called myMethodName is defined in here it will be invoked and the resulting html
	* will replace the placeholder.
	*/
	